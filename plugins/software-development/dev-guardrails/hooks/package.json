{
  "name": "@catylai/dev-guardrails-hooks",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Hook engine for the dev-guardrails plugin: PreToolUse gates plus non-blocking lifecycle and PostToolUse reports. Runs directly from TypeScript with Node's type stripping; no build step and no runtime dependencies.",
  "license": "MIT",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test 'src/**/*.test.ts'",
    "test:coverage": "node --experimental-strip-types --experimental-test-coverage --disable-warning=ExperimentalWarning --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
