# ${PROJECT_NAME}

${ONE_LINE_DESCRIPTION}

## Commands

```bash
make install   # ${INSTALL_COMMAND}
make test      # ${TEST_COMMAND}
make lint      # ${LINT_COMMAND}
make verify    # must pass before a change is done
```

<!-- One line per target the Makefile defines. Terraform-only projects have no test or lint:
     list install, fmt, validate, plan-dev and verify instead. -->

${ADR_SECTION}
<!-- Only when docs/adr/ exists: the "Architecture and decisions" section from adr-init Step 6.
     Otherwise delete this comment and the placeholder above. -->

## Gotchas

<!-- Add project-specific mistakes here as they are discovered: the non-obvious things a new
     session would otherwise get wrong. Delete this comment once the first entry lands. -->
